// Этот плагин теперь только для клиента
// Основная проверка перенесена в plugins/supabase-init.ts (работает и на сервере, и на клиенте)
export default defineNuxtPlugin(() => {
  // Дополнительная проверка только на клиенте
  // Основная логика в supabase-init.ts
})

