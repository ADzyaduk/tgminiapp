# История изменений и оптимизаций

## Оптимизации от [дата]

### ✅ Выполнено

1. **Устранено дублирование кода**
   - Вынесена функция `generateTokens` в общий утилитный файл `server/utils/jwt.ts`
   - Удалено дублирование между `server/api/telegram/auth.post.ts` и `server/api/telegram/check-auth.get.ts`

2. **Обновлены зависимости**
   - `@supabase/supabase-js`: 2.49.8 → 2.81.1
   - `@nuxt/ui`: 3.0.2 → 3.3.7
   - `@nuxtjs/supabase`: 1.5.0 → 1.6.2
   - `pinia`: 3.0.2 → 3.0.4
   - `dayjs`: 1.11.13 → 1.11.19
   - `yup`: 1.6.1 → 1.7.1
   - Обновлены типы и иконки

3. **Упрощена работа с JWT секретами**
   - Теперь `JWT_REFRESH_SECRET` опционален
   - Если не указан, автоматически используется `JWT_SECRET`
   - Добавлена функция `getTokenPayload` для упрощения работы с токенами

4. **Улучшена документация**
   - Обновлен `SETUP.md` с объяснением JWT секретов
   - Создан `OPTIMIZATION_PLAN.md` с планом дальнейших улучшений

### 📝 Объяснение JWT_REFRESH_SECRET

**JWT_REFRESH_SECRET** - это отдельный секретный ключ для подписи refresh токенов.

#### Как это работает:
- **JWT_SECRET** - используется для access токенов (живут 15 минут)
- **JWT_REFRESH_SECRET** - используется для refresh токенов (живут 7 дней)

#### Зачем нужен отдельный секрет?
1. **Безопасность** - если access токен скомпрометирован, refresh токен остается безопасным
2. **Гибкость** - можно отозвать все refresh токены, изменив только JWT_REFRESH_SECRET
3. **Лучшая практика** - разделение секретов для разных типов токенов

#### Упрощенный вариант:
Можно использовать один `JWT_SECRET` для обоих типов токенов. Система автоматически использует `JWT_SECRET`, если `JWT_REFRESH_SECRET` не указан.

### 🔄 Следующие шаги

1. **Установить обновленные зависимости:**
   ```bash
   npm install
   ```

2. **Проверить работу приложения:**
   ```bash
   npm run dev
   ```

3. **Опционально - добавить тесты** (см. OPTIMIZATION_PLAN.md)

### ⚠️ Важные замечания

- Все изменения обратно совместимы
- JWT_REFRESH_SECRET теперь опционален - можно оставить пустым или не указывать
- Обновления зависимостей безопасны (patch/minor версии)
- Major обновления (Nuxt 4, @nuxt/ui 4) требуют дополнительного тестирования

