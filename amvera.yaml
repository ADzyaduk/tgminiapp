meta:
  environment: docker
  toolchain:
    name: docker
build: {}
run:
  # Явно указываем команду запуска (совпадает с CMD в Dockerfile)
  command: node .output/server/index.mjs
  containerPort: "3000"
